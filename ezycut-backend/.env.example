# ============================================================
# EzyCut Backend — Environment Variables
# Copy this file to .env and fill in your values
# ============================================================

PORT=5000

# MongoDB Atlas Connection String
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?appName=EzycutBarberly

# JWT Secret — use a long random string in production (64+ chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_USE_STRONG_SECRET

# Frontend URL for CORS
CLIENT_URL=http://localhost:5173

# Razorpay Keys
# Test keys (rzp_test_*): safe for development only
# Live keys (rzp_live_*): for production
RAZORPAY_KEY_ID=rzp_test_XXXXXXXXXX
RAZORPAY_KEY_SECRET=XXXXXXXXXXXXXXXXXXXXXXXX

# Razorpay Webhook Secret — set in your Razorpay Dashboard > Webhooks
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_here

# ─── Cloudinary ────────────────────────────────────────────────
# Used for KYC document uploads and salon image hosting.
# Get your credentials from: https://cloudinary.com → Dashboard → API Keys
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here
